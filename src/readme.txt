1) On running test1.rb , we get a AST for the program. The program can by run by giving the command 'python parser.py test1.rb' The AST is stored in AST.txt. The symbol table is stored in the Symbol_Table.txt. Test1.rb has some common operations like addition and equation.

2) test2.rb has some errors like undeclared variables error.

3) test3.rb has error like mismatch addition error because it adds integer and string.